let add = (a = 0, b = 0) => {
    return a + b;
}

let sub = (a = 0, b = 0) => {
    return a - b;
}

let city = 'bangalore';

module.exports = {
    // concise property
    add,
    sub,
    city
}